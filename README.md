### Minimal reproduction of the Neovim module loading problem on Termux

Requirements: Rust, Neovim, LuaJIT

Steps to reproduce:

1. Run `./with_luajit.sh`. It works on my device.
2. Run `./with_neovim.sh`. It does not work on my device.
